## To run the app locally
```
git clone https://github.com/ghagevaibhav/hono-app/
cd hono-app
npm install
npm run dev
```

### To deploy on cloudflare worker 
```
If not logged in wrangler, first run
npx wrangler login

and then
npm run deploy
```
